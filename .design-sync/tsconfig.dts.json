{
  "_comment": "Emits a .d.ts tree from the component source so design-sync can extract real prop contracts (synth-from-src mode ships no .d.ts). Output lands in dist/types, which findTypesRoot picks up. Not used by Next or the app build.",
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "noEmit": false,
    "declaration": true,
    "declarationMap": false,
    "emitDeclarationOnly": true,
    "incremental": false,
    "outDir": "../dist/types",
    "rootDir": "../src",
    "skipLibCheck": true
  },
  "include": ["../src/components/**/*.ts", "../src/components/**/*.tsx"],
  "exclude": ["../node_modules", "../.next", "../dist"]
}
